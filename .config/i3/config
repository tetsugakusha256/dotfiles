# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

include ~/.config/i3/i3_theme_mocha

# Set mod key (Mod1=alt, Mod4=super)
set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Hack Nerd Font Mono 24px

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock /home/anon/.local/bin/i3lock-custom.sh

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
# exec --no-startup-id nm-applet

# notification center deamon
exec --no-startup-id deadd-notification-center

# i3-layouts support
# exec_always i3-layouts

# backgrounds wallpaper
# exec --no-startup-id feh --bg-scale /home/anon/Pictures/Wallpapers/Catppuccin/mocha/catppuccin_main.png
# Polybar
exec --no-startup-id polybar -r main
# exec --no-startup-id polybar -r bottom
exec --no-startup-id sleep 0.3;/home/anon/.pyenv/versions/3.12.3/bin/python /home/anon/.local/bin/hide_polybar_mod4.py
#exec --no-startup-id hideIt.sh -N polybar -H -d top

# Input manager (japanese etc...)
# exec --no-startup-id fcitx5 -d

exec --no-startup-id nvidia-settings --assign CurrentMetaMode="nvidia-auto-select +0+0 { ForceFullCompositionPipeline = On }"

exec --no-startup-id picom --config /home/anon/.config/picom/picom.conf -b
# exec --no-startup-id "i3-msg 'workspace 2; append_layout /home/anon/.config/i3/workspace-2.json'"
# exec --no-startup-id "i3-msg 'workspace 5; append_layout /home/anon/.config/i3/workspace-5.json'"
# exec --no-startup-id "i3-msg 'workspace 7; append_layout /home/anon/.config/i3/workspace-7.json'"
exec --no-startup-id i3-msg 'workspace 2; exec vivaldi'
# exec --no-startup-id i3-msg 'workspace 7; exec alacritty -e nvim -c "set textwidth=80" +":cd /home/anon/Documents/myWiki/"'
exec --no-startup-id /home/anon/.local/bin/battery_notification.sh

# Global mapping using xbindkeys saved in ~/.xbindkeysrc
exec --no-startup-id xbindkeys

# exec --no-startup-id "flatpak run com.tomjwatson.Emote"
# Now using interception tools to set the ctrl_modifier remap at a lower level
# it should work in the ttc, wayland and x11
# exec --no-startup-id "/home/anon/.local/bin/capsRemap.sh"

# Sound
# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Change theme
bindsym $mod+1 exec "~/.local/bin/ThemeSwitcher/theme_switcher.sh latte"
bindsym $mod+2 exec "~/.local/bin/ThemeSwitcher/theme_switcher.sh mocha"
bindsym $mod+3 exec "~/.screenlayout/Basic_layout.sh"
bindsym $mod+4 exec "~/.screenlayout/Only_small.sh"
bindsym $mod+5 exec "~/.screenlayout/wide_dp2.sh"
bindsym $mod+6 exec "~/.screenlayout/2_screen.sh"
bindsym $mod+0 exec "~/.local/bin/screenshot_to_last_ankicard.sh"

# redshift
# bindsym $mod+1 exec redshift -O 4000 
# bindsym $mod+2 exec redshift -x 
# bindsym $mod+3 exec redshift -O 2500 

# Screen saver
# bindsym $mod+4 exec xset dpms force off

# Toggle notification center
bindcode $mod+34 exec kill -s USR1 $(pidof deadd-notification-center)
# Open chatgpt
bindsym $mod+slash exec "bt activate `bt list | grep chat.openai | cut -f 1 | head -n 1`;i3-msg workspace 2"
# bindsym $mod+period exec "bt activate `bt list | grep devdocs.io | cut -f 1`;i3-msg workspace 2"
bindsym $mod+period exec rofimoji -r "" -f emojis_animals_nature emojis_component emojis_flags emojis_food_drink emojis_objects emojis_people_body emojis_smileys_emotion emojis_symbols emojis_travel_places emojis_activities math -a clipboard --selector-args="-theme ~/.config/rofi/emoji_chooser.rasi"

# start a terminal
bindsym $mod+t exec /home/anon/.config/rofi/bin/rofiTab.sh
bindsym $mod+q exec /home/anon/.local/bin/ProjectLauncher/project_launcher.sh
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill
bindsym $mod+Shift+w kill

# bindsym --release $mod+z exec 'kill -s SIGUSR1 32242'
# bindcode --release 133 exec i3-sensible-terminal

# start dmenu (a program launcher)
bindsym $mod+s exec /home/anon/.config/rofi/bin/launcher
# Anki card
bindsym $mod+c exec /home/anon/.local/bin/QuickAnki/quick_anki.sh

# TODO:
# bindsym $mod+period exec "flatpak run it.mijorus.smile"

# change focus
bindsym $mod+h focus left
bindsym $mod+n focus down
bindsym $mod+e focus up
bindsym $mod+i focus right
# Ex: if I selected the most left window, focus left will do nothing ...
focus_wrapping no

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left 80px
bindsym $mod+Shift+n move down 80px
bindsym $mod+Shift+e move up 80px
bindsym $mod+Shift+i move right 80px
# bindsym $mod+Ctrl+h exec i3l move left 80px
# bindsym $mod+Ctrl+n exec i3l move down 80px
bindsym $mod+Ctrl+e exec i3l move up 80px
# bindsym $mod+Ctrl+i exec i3l move right 80px

for_window [title="Picture in picture"] floating enable, move absolute position 2027px 45px, resize set width 533px 
# Anki window on workspace 9
assign [title="Anki"] 9

# i3-layouts
set $i3l vstack 0.6 right to workspace $ws1
set $i3l hstack 0.7 down to workspace $ws2
set $i3l 2columns to workspace $ws3
set $i3l hstack 0.6 down to workspace $ws4
set $i3l hstack 0.7 down to workspace $ws5
set $i3l vstack 0.6 right to workspace $ws6
set $i3l vstack 0.6 right to workspace $ws7
set $i3l 2columns to workspace $ws8
set $i3l 2columns to workspace $ws9
set $i3l 2columns to workspace $ws10

# split in horizontal orientation
# bindsym $mod+a exec i3l vstack 0.6 right
# split in vertical orientation
# bindsym $mod+v exec i3l hstack 0.65 down
# split in 2columns mode
bindsym $mod+z exec i3l 2columns

# split in horizontal orientation
bindsym $mod+a split h
# split in vertical orientation
bindsym $mod+v split v

# alt-tab like for workspaces
bindsym Mod4+Tab workspace back_and_forth
bindsym Mod4+BackSpace workspace back_and_forth

# alternatively, you can use the cursor keys:
bindsym $mod+Ctrl+Left move left
bindsym $mod+Ctrl+Down move down
bindsym $mod+Ctrl+Up move up
bindsym $mod+Ctrl+Right move right


# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+q layout stacking
bindsym $mod+p layout toggle split
#bindsym $mod+w layout tabbed

# toggle tiling / floating
bindsym $mod+g floating toggle
# make the toggle between focused and not focused 
bindsym $mod+b sticky toggle
# change focus between tiling / floating windows
bindsym $mod+d focus mode_toggle
bindsym $mod+x focus mode_toggle

# bindsym $mod+g focus floating 
# bindsym $mod+a focus tiling 

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
# bindsym $mod+s focus chi

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"


workspace 1 output primary
workspace 2 output primary
workspace 3 output primary
workspace 4 output primary
workspace 5 output primary
workspace 6 output primary
workspace 7 output primary
workspace 8 output primary
workspace 9 output primary
workspace 10 output primary
workspace 11 output right

# switch to workspace
bindsym $mod+l workspace number $ws1
bindsym $mod+u workspace number $ws2
bindsym $mod+y workspace number $ws3
# bindsym $mod+semicolon workspace number $ws4
bindcode $mod+33 workspace number $ws4
bindsym $mod+o workspace number $ws5
bindsym $mod+k workspace number $ws6
bindsym $mod+m workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+j workspace number $ws10

# brightness settings
# TODO:
bindsym XF86MonBrightnessUp exec "brightnessctl set +3%"
bindsym XF86MonBrightnessDown  exec "brightnessctl set 2%-"
bindcode 235 exec brightnessctl set 1

workspace 1 output DP-0
workspace 2 output DP-0
workspace 3 output DVI-D-0
workspace 4 output DP-0
workspace 5 output DP-0
workspace 6 output DVI-D-0
workspace 7 output DVI-D-0
workspace 8 output DP-0
workspace 9 output DP-0
workspace 10 output DP-0

# move focused container to workspace
bindsym $mod+Ctrl+l move container to workspace number $ws1
bindsym $mod+Ctrl+u move container to workspace number $ws2
bindsym $mod+Ctrl+y move container to workspace number $ws3
# bindsym $mod+Ctrl+semicolon move container to workspace number $ws4
bindcode $mod+Ctrl+33 move container to workspace number $ws4
bindsym $mod+Ctrl+o move container to workspace number $ws5
bindsym $mod+Ctrl+k move container to workspace number $ws6
bindsym $mod+Ctrl+m move container to workspace number $ws7
bindsym $mod+Ctrl+8 move container to workspace number $ws8
bindsym $mod+Ctrl+9 move container to workspace number $ws9
bindsym $mod+Ctrl+j move container to workspace number $ws10

bindsym $mod+Shift+l move container to workspace number $ws1
bindsym $mod+Shift+u move container to workspace number $ws2
bindsym $mod+Shift+y move container to workspace number $ws3
# bindsym $mod+Shift+semicolon move container to workspace number $ws4
bindcode $mod+Shift+33 move container to workspace number $ws4
bindsym $mod+Shift+o move container to workspace number $ws5
bindsym $mod+Shift+k move container to workspace number $ws6
bindsym $mod+Shift+m move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+j move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

bindsym $mod+Shift+f mode "(S)uspend, (L)ogout, (R)eboot, (P)oweroff"
bindsym $mod+w exec /home/anon/.config/rofi/bin/powermenu

# Create Log out, Reboot, Poweroff bindings
mode "(S)uspend, (L)ogout, (R)eboot, (P)oweroff" {
        bindsym r exec --no-startup-id systemctl reboot
        bindsym s exec --no-startup-id systemctl suspend; mode "default"
        bindsym l exit
        bindsym p exec --no-startup-id systemctl poweroff

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# resizing out of rezise mode
bindsym $mod+Ctrl+h move workspace to output left
bindsym $mod+Ctrl+i move workspace to output right
bindsym $mod+Shift+Ctrl+n resize grow height 5 px or 5 ppt
bindsym $mod+Shift+Ctrl+e resize shrink height 5 px or 5 ppt

bindsym $mod+Shift+b gaps bottom all toggle 33
#bindsym $mod+r mode "resize"

# Activate smart borders (always) border if more than 1 window
smart_borders on
smart_gaps off

default_border pixel 2
default_floating_border pixel 1
focus_follows_mouse no
hide_edge_borders smart

# Inner gaps for all windows: space between two adjacent windows (or split containers).
gaps inner 5
gaps outer 0
gaps bottom 0
